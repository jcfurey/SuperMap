# Core
# For live ROS2 mode, prefer the distro's apt numpy/scipy/opencv so the ROS
# Python bindings and this package share one numpy (see docker/Dockerfile);
# image messages are decoded with plain numpy, no cv_bridge needed.
# Offline-only use has no such constraint.
numpy>=1.24
scipy>=1.10
PyYAML>=6.0
opencv-python>=4.8

# Offline example visualization (optional; --live)
rerun-sdk>=0.18

# Detector backends (install the ones you plan to use; see config/semantic_mapping.yaml)
ultralytics>=8.3          # yoloe
# groundingdino-py        # groundingdino: install from https://github.com/IDEA-Research/GroundingDINO
# sam2                    # groundingdino + SAM2 mask refinement: https://github.com/facebookresearch/sam2

# Testing
# Capped below 8: ROS2's own launch_testing pytest plugin registers a pytest11
# hookimpl against the pre-pytest-8 hookspec (path arg, not collection_path)
# and fails to load under pytest 8+ -- pulling in a newer pytest here breaks
# `ros2 test` / `colcon test` in a real workspace, not just this repo's suite.
pytest>=7.4,<8
